# Leave Management Remediation Report

**Phase:** 5 — Leave Management (Remediation Pass)
**Date:** 2026-06-17
**Based on:** docs/13.1-leave-management-validation.md
**Build:** PASS | Typecheck: PASS | Lint: PASS | Tests: 38/38 PASS

---

## Findings Fixed

| ID | Severity | Status |
|----|----------|--------|
| H1 | HIGH | FIXED — cronGuard.ts implemented; applied to all 3 cron routes |
| M2 | MEDIUM | FIXED — halfDayPeriod persisted end-to-end |
| M3 | MEDIUM | FIXED — getLeaveYearBoundaries uses UTC methods |
| M4 | MEDIUM | FIXED — Leave withdrawal implemented |
| M5 | MEDIUM | FIXED — 10 LeaveBalanceService tests + 11 leaveUtils tests |

---

## Files Modified

### Security (H1)
- `src/middleware/cronGuard.ts` — implemented `assertCronAuth(request)`: validates `Authorization: Bearer <CRON_SECRET>` header; returns 401 NextResponse if missing/wrong, 500 if `CRON_SECRET` env not set
- `src/app/admin/cron/session-auto-close/route.ts` — added `assertCronAuth` call at top
- `src/app/admin/cron/leave-year-allocation/route.ts` — added `assertCronAuth` call at top
- `src/app/admin/cron/leave-carryforward-expiry/route.ts` — added `assertCronAuth` call at top

### halfDayPeriod Persistence (M2)
- `src/models/Leave.ts` — added `HalfDayPeriod` type, `halfDayPeriod?: HalfDayPeriod` to ILeave interface and LeaveSchema
- `src/models/AttendanceDay.ts` — added `leaveHalfDayPeriod?: 'morning' | 'afternoon'` to IAttendanceDay and schema
- `src/models/index.ts` — exported `HalfDayPeriod` from Leave
- `src/services/LeaveService.ts`:
  - `apply()` — added `halfDayPeriod?: HalfDayPeriod` to params; stored in `Leave.create()`; returned in response
  - `approve()` — passes `leaveHalfDayPeriod: leave.halfDayPeriod` to `AttendanceDay.findOneAndUpdate` $set
  - `formatLeave()` — added `halfDayPeriod`, `withdrawalReason`, `withdrawnBy`, `withdrawnAt` to output
- `src/app/api/v1/leaves/route.ts` (POST) — forwards `parsed.halfDayPeriod` to `LeaveService.apply()`

### Leave Year Boundary Fix (M3)
- `src/lib/utils/leaveUtils.ts` — replaced `date.getFullYear()` → `date.getUTCFullYear()` and `date.getMonth() + 1` → `date.getUTCMonth() + 1`

### Leave Withdrawal (M4)
- `src/models/Leave.ts` — added `'withdrawn'` to `LeaveStatus` enum; added `withdrawnBy?`, `withdrawnAt?`, `withdrawalReason?` fields to interface and schema
- `src/models/LeaveTransaction.ts` — added `'restoration-withdrawal'` to `LeaveTransactionType` and schema enum
- `src/validators/leave.ts` — added `WithdrawLeaveSchema` (optional reason, max 500)
- `src/services/LeaveService.ts` — added `withdraw()` method:
  - Employee-initiated (ownership check enforced)
  - Status guard: `approved` only → LVE_008
  - Date guard: `affectedDates[0] > today` — leaves that have already started cannot be withdrawn
  - Balance restored to `currentYear` (same policy as revoke)
  - `AttendanceDay` status reverted
  - `LeaveTransaction` `restoration-withdrawal` created in transaction
  - `AuditLog` `LEAVE_WITHDRAWN` created
- `src/app/api/v1/leaves/[id]/withdraw/route.ts` — NEW: PATCH endpoint, employee-accessible, validates body with `WithdrawLeaveSchema`

---

## Tests Added

### leaveUtils (11 tests — `src/__tests__/leave/leaveUtils.test.ts`)
| ID | Scenario |
|----|----------|
| U-UTIL-01 | Jan fiscal year, June date → leaveYear = current year |
| U-UTIL-02 | Jan 1 UTC midnight — no local-time ambiguity |
| U-UTIL-03 | Apr fiscal year, June date → leaveYear = current year |
| U-UTIL-04 | Apr fiscal year, March date → leaveYear = prior year |
| U-UTIL-05 | Apr 1 UTC midnight (boundary start) → leaveYear = current year |
| U-UTIL-06 | Mar 31 UTC midnight (last day of prior year) → leaveYear = 2025 |
| U-UTIL-07 | dateRange single-day → one entry |
| U-UTIL-08 | dateRange Mon–Fri → 5 entries, correct bounds |
| U-UTIL-09 | weekdayOf 2026-01-05 → monday |
| U-UTIL-10 | weekdayOf 2026-01-10 → saturday |
| U-UTIL-11 | weekdayOf 2026-01-11 → sunday |

### LeaveBalanceService (10 tests — `src/__tests__/leave/LeaveBalanceService.test.ts`)
| ID | Scenario |
|----|----------|
| U-LBS-01 | allocateLeaveYear — full allocation, 3 allocations + 3 transactions created |
| U-LBS-02 | allocateLeaveYear — pro-ration for mid-year joiner: isProRated=true, days < annual |
| U-LBS-03 | allocateLeaveYear — duplicate key 11000 → skipped (idempotency) |
| U-LBS-04 | allocateLeaveYear — annualAllocation=0 → allocatedDays=0 |
| U-LBS-05 | processCarryForwardExpiry — zeroes CF, creates expiry transaction with -days |
| U-LBS-06 | processCarryForwardExpiry — no expired employees → 0 processed |
| U-LBS-07 | creditCarryForward — maxDays cap respected (currentYear 20, maxDays 5 → credits 5) |
| U-LBS-08 | creditCarryForward — disabled carryForward types skipped (only paidLeave credited) |
| U-LBS-09 | allocateLeaveYear — veteran employee (joined before year start) → isProRated=false |
| U-LBS-10 | allocateLeaveYear — 2 employees × 3 types = 6 allocations |

**Total: 38 tests across 3 test files (all PASS)**

---

## Remaining Findings

### MEDIUM (not addressed in this pass — recommended for follow-up)

| ID | Finding | Action |
|----|---------|--------|
| M1 | LVE_005 used for "wrong status" in approve/reject; semantically should be LVE_WRONG_STATUS | Deferred — no functional impact, code still unique |
| ~~M2~~ | ~~halfDayPeriod~~ | FIXED |
| ~~M3~~ | ~~getLeaveYearBoundaries UTC bug~~ | FIXED |
| ~~M4~~ | ~~Leave withdrawal~~ | FIXED |
| ~~M5~~ | ~~LeaveBalanceService tests~~ | FIXED |

### LOW (deferred)

| ID | Finding |
|----|---------|
| L1 | reason optional in validator but model has required:true — allows empty string |
| L2 | errors.ts LEAVE_001-005 constants outdated and unused |
| L3 | Leave.remarks field unused |
| L4 | restoration-rejection/cancellation enum values unused |

---

## Verification

```
Lint:       PASS (0 errors)
Typecheck:  PASS (0 errors)
Build:      PASS (clean)
Tests:      38/38 PASS
  LeaveService.test.ts:        17/17
  LeaveBalanceService.test.ts: 10/10
  leaveUtils.test.ts:          11/11
```
